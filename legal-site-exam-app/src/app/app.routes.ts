import { Routes } from '@angular/router';
import { SpeechManagerComponent } from './pages/speech-manager.component';

export const routes: Routes = [
    {
        path: '',
        component: SpeechManagerComponent
    }
]